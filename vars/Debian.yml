vagrant_file: "vagrant_{{vagrant_version}}_{{vagrant_arch_ext}}.deb"
vagrant_url: "https://releases.hashicorp.com/vagrant/{{vagrant_version}}/{{vagrant_file}}"
vagrant_tmp_file: "{{vagrant_tmp}}/{{vagrant_file}}"
